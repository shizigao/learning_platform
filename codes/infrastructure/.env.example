# Copy this file to infrastructure/.env and replace every placeholder.
# infrastructure/.env is ignored by Git. Never send its values to AI.

REDIS_PASSWORD=replace_with_a_strong_local_redis_password
MINIO_ROOT_USER=replace_with_a_local_minio_admin_name
MINIO_ROOT_PASSWORD=replace_with_a_strong_local_minio_password

